## Auto-generated by Store.ControlPlane
## Tenant: Bridge Mall (bridgemall)

http:
  routers:
    bridgemall-ui:
      rule: "Host(`bridgemall.store.127.0.0.1.nip.io`) || Host(`bridgemall.store.clexan.com`)"
      service: "bridgemall-ui"
      entryPoints:
        - "web"
        - "websecure"

    bridgemall-api:
      rule: "Host(`api.bridgemall.store.127.0.0.1.nip.io`) || Host(`api.bridgemall.store.clexan.com`)"
      service: "bridgemall-api"
      entryPoints:
        - "web"
        - "websecure"

  services:
    bridgemall-ui:
      loadBalancer:
        servers:
          - url: "http://bridgemall-ui:8080"

    bridgemall-api:
      loadBalancer:
        servers:
          - url: "http://bridgemall-api:8080"
