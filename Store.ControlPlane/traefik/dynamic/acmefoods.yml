## Auto-generated by Store.ControlPlane
## Tenant: Acme Supermarket (acmefoods)

http:
  routers:
    acmefoods-ui:
      rule: "Host(`acmefoods.store.127.0.0.1.nip.io`) || Host(`acmefoods.store.clexan.com`)"
      service: "acmefoods-ui"
      entryPoints:
        - "web"
        - "websecure"

    acmefoods-api:
      rule: "Host(`api.acmefoods.store.127.0.0.1.nip.io`) || Host(`api.acmefoods.store.clexan.com`)"
      service: "acmefoods-api"
      entryPoints:
        - "web"
        - "websecure"

  services:
    acmefoods-ui:
      loadBalancer:
        servers:
          - url: "http://acmefoods-ui:8080"

    acmefoods-api:
      loadBalancer:
        servers:
          - url: "http://acmefoods-api:8080"
